{
  "name": "offline-collab-whiteboard",
  "version": "1.0.0",
  "private": true,
  "description": "Offline collaborative whiteboard with optimistic operations, causal dependencies, and rollback/retry.",
  "scripts": {
    "test": "node tests/core.test.js"
  }
}
